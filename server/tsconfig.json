{
  "compilerOptions": {
    // "module": "commonjs",
    "isolatedModules": false,
    "esModuleInterop": true,
    "outDir": "dist",
    "lib": ["es2021", "dom"],
    "types": [
     "node"
      ]
  },

  "include": ["src/**/*.ts", "src/utils/twilio/verifySMS.ts"],
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ],

  "ts-node": {
    // It is faster to skip typechecking.
    // Remove if you want ts-node to do typechecking.
    "transpileOnly": true,

    "files": true
  }
}
